# TuxSec Agent Requirements (v2.0 - New Architecture)

# Core dependencies
PyYAML>=6.0.0
httpx>=0.24.0

# Async web server (for push mode)
aiohttp>=3.8.0

# Optional: For better system info
distro>=1.8.0

# Old dependencies (kept for compatibility with legacy code)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
structlog>=23.0.0
cryptography>=41.0.0